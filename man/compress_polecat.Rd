% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compress.R
\name{compress_polecat}
\alias{compress_polecat}
\title{Compress or decompress local POLECAT files}
\usage{
compress_polecat(data_dir = NULL, action = "compress", remove = TRUE, verbose = TRUE)
}
\arguments{
\item{data_dir}{Path to the local data directory. If \code{NULL}, uses the
directory set via \code{\link{setup_polecat}}.}

\item{action}{One of \code{"compress"} (default) or \code{"decompress"}.}

\item{remove}{Logical; remove the original files after compression/
decompression? Defaults to \code{TRUE}.}

\item{verbose}{Print progress messages?}
}
\value{
Invisibly returns a character vector of the resulting file paths.
}
\description{
Compress uncompressed \code{.txt} files to \code{.gz} format to save disk
space, or decompress \code{.gz} files back to \code{.txt}. The
\code{\link{read_polecat}} function handles \code{.gz} files transparently.
}
\examples{
\dontrun{
  # Compress all .txt files
  compress_polecat("~/data/polecat")

  # Decompress back
  compress_polecat("~/data/polecat", action = "decompress")
}
}
