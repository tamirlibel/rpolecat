% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{normalize_polecat}
\alias{normalize_polecat}
\title{Normalize POLECAT data}
\usage{
normalize_polecat(
  x,
  parse_dates = TRUE,
  add_year = TRUE,
  add_yearmonth = TRUE,
  lowercase_types = TRUE
)
}
\arguments{
\item{x}{A data frame of POLECAT events.}

\item{parse_dates}{Logical; convert \code{story_date} to \code{Date} class?
Defaults to \code{TRUE}.}

\item{add_year}{Logical; add a \code{year} column? Defaults to \code{TRUE}.}

\item{add_yearmonth}{Logical; add a \code{year_month} column
(format \code{"YYYY-MM"})? Defaults to \code{TRUE}.}

\item{lowercase_types}{Logical; lowercase \code{event_type}, \code{mode},
and \code{context}? Defaults to \code{TRUE}.}
}
\value{
The input data frame with normalization applied.
}
\description{
Apply standard cleaning and normalization to a POLECAT data frame:
parse dates, ensure consistent casing, trim whitespace, and add
convenience columns.
}
\examples{
data(polecat_sample)
dat <- normalize_polecat(polecat_sample)
class(dat$story_date)
head(dat$year)
head(dat$year_month)
}
