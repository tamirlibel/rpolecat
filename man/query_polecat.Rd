% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database.R
\name{query_polecat}
\alias{query_polecat}
\title{Query the POLECAT database}
\usage{
query_polecat(query, db_path = NULL, backend = NULL)
}
\arguments{
\item{query}{A SQL query string.}

\item{db_path}{Path to the database file. If \code{NULL}, uses the path
set via \code{\link{setup_polecat}}.}

\item{backend}{Database backend: \code{"duckdb"} or \code{"sqlite"}.}
}
\value{
A data frame with query results.
}
\description{
Execute a SQL query against a POLECAT database and return the results as
a data frame.
}
\examples{
\dontrun{
  # Count events by year
  query_polecat("SELECT substr(story_date, 1, 4) as year, COUNT(*) as n
                 FROM events GROUP BY year ORDER BY year")

  # Events involving the USA
  query_polecat("SELECT * FROM events
                 WHERE source_country = 'USA' OR target_country = 'USA'
                 LIMIT 100")
}
}
