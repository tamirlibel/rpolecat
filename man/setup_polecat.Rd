% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{setup_polecat}
\alias{setup_polecat}
\title{Set up rpolecat}
\usage{
setup_polecat(data_dir, use_db = FALSE, db_path = NULL, r_profile = FALSE)
}
\arguments{
\item{data_dir}{Path to the local directory where POLECAT data files are
stored.}

\item{use_db}{Logical; use a database backend? Defaults to \code{FALSE}.}

\item{db_path}{Path to the database file (only used when \code{use_db = TRUE}).}

\item{r_profile}{Logical; write these settings to your user-level
\code{.Rprofile} so they persist across sessions? Defaults to \code{FALSE}.}
}
\value{
Invisibly returns a list of the options that were set.
}
\description{
Persist the local data directory so you don't have to pass it to every
function call. Optionally writes the setting to your \code{.Rprofile} so
it is available in future R sessions.
}
\details{
The following R options are set:
\itemize{
  \item \code{rpolecat.data_dir}: path to local data directory
  \item \code{rpolecat.use_db}: whether to use a database backend
  \item \code{rpolecat.db_path}: path to the database file
}
}
\examples{
\dontrun{
  setup_polecat("~/data/polecat")

  # Persist across sessions
  setup_polecat("~/data/polecat", r_profile = TRUE)
}
}
