% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.R
\name{filter_polecat}
\alias{filter_polecat}
\title{Filter POLECAT events}
\usage{
filter_polecat(
  x,
  countries = NULL,
  src_countries = NULL,
  tgt_countries = NULL,
  years = NULL,
  event_types = NULL,
  contexts = NULL,
  modes = NULL,
  geolocated = NULL
)
}
\arguments{
\item{x}{A data frame of POLECAT events.}

\item{countries}{Character vector of ISO3 country codes to keep. Matches
against both \code{source_country} and \code{target_country}; an event is
kept if either the source or target matches.}

\item{src_countries}{Character vector of ISO3 country codes to match on
\code{source_country} only.}

\item{tgt_countries}{Character vector of ISO3 country codes to match on
\code{target_country} only.}

\item{years}{Numeric vector of years or a length-2 vector interpreted as a
range. Filters on \code{story_date}.}

\item{event_types}{Character vector of PLOVER event types to keep
(e.g. \code{"protest"}, \code{"assault"}).}

\item{contexts}{Character vector of context values to keep. Matches events
where the \code{context} column contains any of the specified values
(handles semicolon-separated multi-value cells).}

\item{modes}{Character vector of event modes to keep.}

\item{geolocated}{Logical; if \code{TRUE}, keep only events with non-missing
latitude/longitude. If \code{FALSE}, keep only events without coordinates.
If \code{NULL} (default), no filtering on geolocation.}
}
\value{
A filtered data frame.
}
\description{
Convenience function for common filtering operations on a POLECAT data frame.
}
\examples{
data(polecat_sample)
# Events involving the USA
usa <- filter_polecat(polecat_sample, countries = "USA")

# Protests and assaults in 2023
violent <- filter_polecat(polecat_sample,
                          event_types = c("protest", "assault"),
                          years = 2023)

# Only geolocated events
geo <- filter_polecat(polecat_sample, geolocated = TRUE)
}
