% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/country-codes.R
\name{polecat_to_gwcode}
\alias{polecat_to_gwcode}
\title{Convert country codes to Gleditsch-Ward codes}
\usage{
polecat_to_gwcode(x, from = "iso3c", col = c("source_country", "target_country"))
}
\arguments{
\item{x}{A data frame of POLECAT events, or a character vector of
country codes/names.}

\item{from}{The coding scheme of the input. Defaults to \code{"iso3c"}.
See \code{\link[countrycode]{countrycode}} for all options.}

\item{col}{When \code{x} is a data frame, which column(s) to translate.
Defaults to \code{c("source_country", "target_country")}.}
}
\value{
If \code{x} is a data frame, returns it with added
  \code{source_gwcode} and/or \code{target_gwcode} columns. If \code{x} is
  a character vector, returns a numeric vector of Gleditsch-Ward codes.
}
\description{
Translate POLECAT country identifiers to the Gleditsch & Ward numeric state
codes commonly used in political science datasets. Uses the \pkg{countrycode}
package.
}
\examples{
data(polecat_sample)
dat <- polecat_to_gwcode(polecat_sample)
head(dat[, c("source_country", "source_gwcode",
             "target_country", "target_gwcode")])

# Translate a vector
polecat_to_gwcode(c("USA", "GBR", "FRA"))
}
