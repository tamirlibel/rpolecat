% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{expand_polecat_col}
\alias{expand_polecat_col}
\title{Expand a multi-value column into long format}
\usage{
expand_polecat_col(x, col, sep = ";")
}
\arguments{
\item{x}{A data frame of POLECAT events.}

\item{col}{The name of the column to expand (as a string).}

\item{sep}{The delimiter used to separate values. Defaults to \code{";"}.}
}
\value{
A data frame in long format with one row per value in \code{col}.
All other columns are repeated for each value.
}
\description{
POLECAT data sometimes stores multiple values in a single cell, separated
by a delimiter (commonly a semicolon). This function splits those cells and
returns a data frame in long format, with one row per value.
}
\examples{
\dontrun{
  dat <- read_polecat("my/data/dir")

  # Expand the "context" column (may contain e.g. "military;economic")
  dat_long <- expand_polecat_col(dat, "context")
}
}
